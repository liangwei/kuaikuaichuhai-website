# Strapi CMS 配置
# 使用 127.0.0.1 而不是 localhost 避免 IPv6 连接问题
NEXT_PUBLIC_STRAPI_URL=http://127.0.0.1:1337
STRAPI_API_URL=http://127.0.0.1:1337/api

# Next.js 环境变量
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# 缓存策略配置
# 开发环境自动不缓存，生产环境默认 0（实时更新）
# 如需缓存以提高性能，设置为秒数，例如：60（1分钟）、300（5分钟）
NEXT_PUBLIC_REVALIDATE_TIME=0
